#!/usr/bin/env python3

from setuptools import setup, find_packages

setup(
    name='dev_edit_install',
    version="0.0.0+editable",
    description="Editable install of local scripts/libraries",
    packages=find_packages(),
)

